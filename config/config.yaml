# 胶片伴侣 AI - 配置文件

# ↓↓↓ 旧的 DeepSeek 配置（注释保留备用） ↓↓↓
# llm:
#   provider: deepseek
#   model: deepseek-v4-flash
#   temperature: 0.3
#   max_tokens: 4000

# ↑↑↑ 旧的 DeepSeek 配置 ↑↑↑

# 火山引擎 Doubao 配置
llm:
  provider: doubao
  model: doubao-seed-2-0-lite-260428
  base_url: "https://ark.cn-beijing.volces.com/api/v3"
  api_key: "25304a4d-ae74-4c0c-ba8f-ab8220a49229"
  temperature: 0.3
  max_tokens: 4000

database:
  url: postgresql+asyncpg://filmuser:filmpass@localhost:5432/film_companion
  pool_size: 5
  max_overflow: 10

memory:
  short_term_max_rounds: 20   # 短期上下文保留轮次
  enable_long_term: true      # 启用长期记忆

plugins:
  auto_load: true             # 自动加载 plugins/ 下的所有插件
  dir: plugins
